[package]
name = "fizz_conversation_history"
version = "0.1.0"
description = "Fizz brain — windowed in-memory turn history (cap N turns). Conversation doesn't carry topics across resets; DB memory is separate."
